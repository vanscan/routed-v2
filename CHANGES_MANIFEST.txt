RouTeD — GitHub sync status & changed files
===========================================
Updated: 2026-06-06

CURRENT STATUS
--------------
Production at https://api.getrouted.xyz is HEALTHY and serving all expected
endpoints (verified 2026-06-06). See COOLIFY_SETUP.md §0 for full smoke-test
results.

The Replit workspace main is 1 commit ahead of github.com/vanscan/routed-v2.
Outbound git push from Replit times out (network restriction). "Save to GitHub"
returns a non-fast-forward 500.

PENDING WORKSPACE-ONLY COMMIT
------------------------------
Commit: "Update backend dependencies for improved functionality"
File changed: backend/requirements.txt
Change: removed the 'emergentintegrations' package line (package migrated away)

This is a clean-up commit — production is already running without that package.
It does NOT affect any runtime behaviour; the container builds fine without it.

HOW TO SYNC TO GITHUB
---------------------
Option A — from a machine with GitHub credentials:
  1) git clone https://github.com/vanscan/routed-v2
  2) Remove 'emergentintegrations' from backend/requirements.txt
  3) git add backend/requirements.txt
  4) git commit -m "Remove emergentintegrations dep; clean requirements"
  5) git push origin main
  6) In Coolify dashboard: Redeploy (optional — prod is already healthy)

Option B — store GITHUB_TOKEN secret in Replit, then from Replit shell:
  git remote set-url origin https://$GITHUB_TOKEN@github.com/vanscan/routed-v2
  git push origin main

Option C (original — GitHub web UI):
  Open your repo → navigate to backend/requirements.txt → Edit →
  remove the emergentintegrations line → commit to main.

NOTE: Do NOT upload node_modules, .expo, caches, or .env files.

PREVIOUSLY CHANGED FILES (all now live in production)
------------------------------------------------------
Native MapLibre migration (Phase 0-3):
  frontend/src/components/map/DeliveryMapNative.native.tsx
  frontend/src/components/map/DeliveryMapNative.tsx
  frontend/src/components/map/DeliveryMapNative.types.ts
  frontend/src/components/map/mapTileLoaders.ts
  frontend/src/components/map/mapEditingHelpers.ts
  frontend/src/components/DeliveryMap.native.tsx
  frontend/src/utils/featureFlags.ts
  frontend/app/_layout.tsx
  frontend/app/native-map-test.tsx
  frontend/app/(tabs)/index.tsx
  frontend/app.json
  frontend/eas.json
  frontend/package.json, frontend/package-lock.json

Backend (all verified live at api.getrouted.xyz):
  backend/server.py          (OSRM circuit-breaker fix + tile/housenumber endpoints)
  backend/routes/tiles.py    (parcels + addresses tile routes)
  backend/routes/housenumbers.py  (housenumber endpoint)
  backend/tests/*            (related tests)

Docs:
  COOLIFY_SETUP.md           (updated with deployment status, smoke tests, sync steps)
  CHANGES_MANIFEST.txt       (this file)
